import {AppShell} from "@/components/AppShell";import {getSandboxState} from "@/lib/sandbox";import {formatUsd} from "@/lib/money";export const dynamic="force-dynamic";export default function ActivityPage(){const s=getSandboxState();const l=s.activeLoan;return <AppShell><section className="hero"><p className="eyebrow">Activity</p><h1>Your money trail.</h1><p className="muted">Only provider-confirmed settlement should appear as collected in production.</p></section><section className="card list"><div className="item"><span>Owner capital seeded</span><strong>{formatUsd(s.capital.totalCapitalCents)}</strong></div>{l&&<div className="item"><span>Sandbox loan {l.status.toLowerCase()}</span><strong>{formatUsd(l.principalCents)}</strong></div>}{s.interestCollectedCents>0&&<div className="item"><span>Interest collected</span><strong>{formatUsd(s.interestCollectedCents)}</strong></div>}</section></AppShell>}
