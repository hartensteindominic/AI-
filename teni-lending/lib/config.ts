export type LendingMode="sandbox"|"production";
function envInt(name:string,fallback:number){const raw=process.env[name];if(!raw)return fallback;const parsed=Number.parseInt(raw,10);if(!Number.isSafeInteger(parsed)||parsed<0)throw new Error(`${name} must be a non-negative integer`);return parsed;}
export const lendingConfig={mode:(process.env.LENDING_MODE==="production"?"production":"sandbox") as LendingMode,maxLoanCents:envInt("INITIAL_MAX_LOAN_CENTS",1000),initialCapitalCents:envInt("INITIAL_CAPITAL_CENTS",10000),minimumReserveCents:envInt("MINIMUM_CAPITAL_RESERVE_CENTS",2000),aprBps:envInt("SANDBOX_APR_BPS",3600),termDays:envInt("SANDBOX_TERM_DAYS",14),lendingEnabled:process.env.LENDING_ENABLED!=="false"};
export function assertProductionReady(){if(lendingConfig.mode!=="production")return;const required=["DATABASE_URL","AUTH_SECRET","IDENTITY_PROVIDER","PAYMENT_PROVIDER","BANK_PROVIDER","DISBURSEMENT_PROVIDER"];const missing=required.filter(k=>!process.env[k]||process.env[k]==="sandbox");if(missing.length)throw new Error(`Production lending is disabled. Missing production configuration: ${missing.join(", ")}`);}
