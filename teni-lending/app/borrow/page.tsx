import Link from "next/link";import {AppShell} from "@/components/AppShell";import {BorrowFlow} from "@/components/BorrowFlow";export default function BorrowPage(){return <AppShell><section className="hero"><Link href="/" className="muted">Back</Link><p className="eyebrow" style={{marginTop:18}}>Borrow</p><h1>Choose your cash.</h1><p className="muted">The server recalculates every amount. Browser changes cannot raise the $10 limit.</p></section><BorrowFlow/></AppShell>}
