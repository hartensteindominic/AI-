import {describe,expect,it} from "vitest";import {assertCanOriginate,availableToOriginate,canTransition,createLoanOffer} from "../lib/domain";import {simpleInterestCents} from "../lib/money";const capital={totalCapitalCents:10000,reservedCents:0,outstandingPrincipalCents:0,lendingPaused:false};describe("loan controls",()=>{it("enforces the $10 server-side maximum",()=>{expect(()=>createLoanOffer(1001)).toThrow(/maximum/);expect(createLoanOffer(1000).principalCents).toBe(1000)});it("uses integer-cent interest math",()=>expect(simpleInterestCents(1000,3600,14)).toBe(14));it("keeps the configured reserve liquid",()=>expect(availableToOriginate(capital)).toBe(8000));it("blocks a second active loan",()=>expect(()=>assertCanOriginate(capital,500,true)).toThrow(/active loan/));it("blocks new loans while paused",()=>expect(()=>assertCanOriginate({...capital,lendingPaused:true},500,false)).toThrow(/paused/));it("validates loan state transitions",()=>{expect(canTransition("OFFERED","ACCEPTED")).toBe(true);expect(canTransition("OFFERED","REPAID")).toBe(false)})});
