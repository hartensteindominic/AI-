"use client";import {useState} from "react";export function PauseButton({paused}:{paused:boolean}){const[message,setMessage]=useState("");async function toggle(){const r=await fetch("/api/admin/pause",{method:"POST",headers:{"content-type":"application/json","x-sandbox-role":"OWNER"},body:JSON.stringify({paused:!paused})});const j=await r.json();if(!j.ok)return setMessage(j.error);window.location.reload()}return <div><button className={paused?"secondary":"danger"} onClick={toggle}>{paused?"Resume new lending":"Pause new lending"}</button>{message&&<p className="notice">{message}</p>}</div>}
